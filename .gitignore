.DS_Store
.vscode/
.idea/
*.log
.wrangler/
serve.command

# Worker secrets are managed via `wrangler secret put` and never committed.
# These patterns guard against an accidental local copy ending up in git.
.dev.vars
.dev.vars.*
secrets.local.*
